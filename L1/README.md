Finds the first 50 prime numbers and stores them in a list.

Prints that list.

Creates a list of the first 50 even numbers with (2..100 step 2).toList()

Adds them to the prime list.

Prints the combined list of the prime numbers and the first 50 even numbers.
